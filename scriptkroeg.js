const age=16;


    if (age>18) { console.log ("je mag naar binnen")
        
    }  else { console.log ("sorry, alleen 18+")
        
    }

    //krijg bovenstaande code niet goed - kun je geen = teken gebruiken bij if statements?

const isFem = true;

if (isFem) { console.log ("je mag naar binnen")
    
} else 
   (isFem); {
       
    console.log ("sorry, ladies only vanavond")
}

const driverStatus="bob"


if (driverStatus=="bob") {console.log ("je mag rijden")
    
} else; {console.log ("bestel een taxi")
    
}

